#include "SkillEvent.h"

bool SkillEvent::Initialize(Api::iEngine* const engine) {
    return true;
}

bool SkillEvent::Launch(Api::iEngine* const engine) {
    return true;
}

bool SkillEvent::Destroy(Api::iEngine* const engine) {
    return true;
}
